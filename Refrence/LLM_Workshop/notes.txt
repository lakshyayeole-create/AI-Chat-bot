INGESTION: 
Load a file, break into chunks , embed them using vectors, then store and finally retrieve

embedding means converting text into numbers
we store the vectorized data
Langchain is a framework which eases the task for making a RAG pipeline
os and sys libraries are imported to load the files
chunk size(the number of words per division/chunk) and chunk overload(the set of tokens which will be common between two consecutive chunk)

DAY 2

we use cosine similarity for to find good context for llm
small angle = more closer chunk
large angle = farther the chunk context
five ways to chunk a file:
1)Character text splitter(day 1)
2)Recursive text splitter
3)Semantic chunking
4)
5)

5 things to know while prompting to a LLM:
role, context, task, constraints, output

top_k value means how many similar chunks to fetch for similarities
GENGESTION: 